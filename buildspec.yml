version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20

  pre_build:
    commands:
      - echo Installing dependencies
      - npm install

  build:
    commands:
      - echo Building the React app
      - npm run build

artifacts:
  files:
    - "**/*"
  base-directory: build
